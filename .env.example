# ========================================================
# PLP Faculty Profiling System - Environment Configuration
# ========================================================

# Application Settings
APP_NAME="PLP Faculty Profiling System"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://faculty-profiling-system.onrender.com

# Database Configuration (TiDB Cloud / Aiven / Railway / Local MySQL)
DB_HOST=gateway01.ap-southeast-1.prod.aws.tidbcloud.com
DB_PORT=4000
DB_USER=your_username.root
DB_PASSWORD=your_strong_password
DB_NAME=finalproj

# Optional: Full Database Connection String (Alternative to discrete DB_* vars)
# DATABASE_URL=mysql://your_username:your_password@your_host:4000/finalproj

# Optional SSL Certificate Authority (leave empty if not needed)
# DB_SSL_CA=/etc/ssl/certs/ca-certificates.crt

# SMTP Email Configuration (Brevo / Gmail / SendGrid / Custom SMTP)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=8cc35a002@smtp-brevo.com
SMTP_PASS=JSh1qV4zbR7DWaI0
SMTP_SECURE=tls
SMTP_FROM_EMAIL=plp.no.reply1@gmail.com
SMTP_FROM_NAME="PLP Faculty Portal"

